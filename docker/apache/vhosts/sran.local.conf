<VirtualHost *:80>
    ServerName sran.local
    ServerAlias www.sran.local

    DocumentRoot /var/www/sran/public
    <Directory /var/www/sran/public>
        AllowOverride None
        Order Allow,Deny
        Allow from All

        FallbackResource /index.php
    </Directory>

    # uncomment the following lines if you install assets as symlinks
    # or run into problems when compiling LESS/Sass/CoffeeScript assets
    # <Directory /var/www/project>
    #     Options FollowSymlinks
    # </Directory>

    # optionally disable the fallback resource for the asset directories
    # which will allow Apache to return a 404 error when files are
    # not found instead of passing the request to Symfony
    <Directory /var/www/sran/public/css>
        FallbackResource disabled
    </Directory>
    <Directory /var/www/sran/public/fonts>
            FallbackResource disabled
    </Directory>
    ErrorLog /var/www/sran/var/log/apache_error.log
    CustomLog /var/www/sran/var/log/apache_access.log combined

    # optionally set the value of the environment variables used in the application
    #SetEnv APP_ENV prod
    #SetEnv APP_SECRET <app-secret-id>
    #SetEnv DATABASE_URL "mysql://db_user:db_pass@host:3306/db_name"
</VirtualHost>